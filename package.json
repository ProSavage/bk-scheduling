{
	"version": "1.0.0",
  "private": true,
  "workspaces": [
  	"packages/*"
  ],
  "scripts": {
    "dev": "cd packages/common && yarn build-watch & cd packages/frontend && yarn dev & cd packages/backend && yarn dev"
  }
}
